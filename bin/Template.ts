export default class Template{

    /**
     * handle :   
     * An event handler that runs automatically when the Template is drawn on the screen.  
     * This event is executed only when rendered.
     */
    handle(){}


    /**
     * handleAlways :  
     * An event handler that runs automatically when the Template is displayed on screen.  
     * This event is always executed even if the same Template has already been rendered..
     */
    handleAlways(){}
}